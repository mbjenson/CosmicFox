#pragma once
class SwordEnemy
{
	/*
	if (!swordTex.loadFromFile("Textures/swordSlash1.png"))
		return false;
	sword = Sword(19, sf::Vector2f(20, 24), swordTex, sf::Vector2u(42, 32), 3, 0, 60.f, 2000, 200, 1);
	sword.initSword();


	if (attackClock.getElapsedTime().asMilliseconds() > attackCooldown) {
				attacking = sword.swing(distVec);
			}
	
	
	if (attackClock.getElapsedTime().asMilliseconds() > attackCooldown) {
				attacking = sword.swing(distVec);
			}
	
	
	
	
	
	*/
};

